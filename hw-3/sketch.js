//create a function named setup
function setup() {
//Setting the resolution to that of my first PC monitor, a classic CRT
  createCanvas ( 800, 600 );
// Creating a non-popping beige background
  background( 'beige' );
}
//filling the canvas with text content
function draw() {
//Say hello to the world
  text( "Hello, World!", 10, 15 );
//Ask the user how they're doing
  text( "How are you?", 20, 30 );

}
